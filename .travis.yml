language: generic
sudo: required
dist: trusty

os:
  - linux

install:
  - sudo add-apt-repository ppa:jonathonf/lazarus -y
  - sudo apt-get update
  - sudo apt-get install lazarus fpc

script:
  - cp environmentoptions.xml Utils/RXXPacker/
  - cd Utils/RXXPacker/
  - sudo lazbuild -r --ws=gtk2 --pcp=./ RXXPacker.lpi --verbose

notifications:
  email:
    on_success: false
    on_failure: false
